{
  "name": "app",
  "version": "1.0.0",
  "description": "UI for the firmware of LSC",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "abbys",
  "license": "ISC",
  "devDependencies": {
    "electron": "^40.4.1"
  },
  "dependencies": {
    "serialport": "^13.0.0"
  }
}
